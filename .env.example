CONVEX_URL=https://YOUR_DEPLOYMENT.convex.cloud

AUTH_JWT_SECRET=replace-with-a-long-random-secret
# Optional:
# AUTH_SESSION_TTL_DAYS=7
# AUTH_COOKIE_NAME=linkstore_session

# Google OAuth (required for authentication)
# Authorize this callback in Google Cloud Console:
# http://localhost:3000/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional comma-separated allowlist for trusted browser origins that can call mutating APIs.
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Razorpay billing (required for subscriptions)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# 32+ bytes secret for encrypting/hashing payment identifiers at rest.
# Example (hex): openssl rand -hex 32
PAYMENTS_DATA_KEY=

# Optional secret for hashing coupon codes and redemption markers.
# Example: openssl rand -hex 32
COUPON_HASH_SECRET=

# Optional env-managed coupon for one free month of store access.
# Change this value and restart the app to rotate the live coupon code.
SUBSCRIPTION_FREE_MONTH_COUPON_CODE=
# Optional label shown in internal records.
# SUBSCRIPTION_FREE_MONTH_COUPON_LABEL=Launch Free Month
# Required global redemption cap when the env-managed coupon is enabled.
# SUBSCRIPTION_FREE_MONTH_COUPON_MAX_REDEMPTIONS=100
# Required expiry as epoch milliseconds or ISO datetime when the env-managed coupon is enabled.
# SUBSCRIPTION_FREE_MONTH_COUPON_EXPIRES_AT=2026-12-31T23:59:59+05:30

# Amazon Product Advertising API (optional, recommended for Amazon links)
# AMAZON_PAAPI_ACCESS_KEY=
# AMAZON_PAAPI_SECRET_KEY=
# AMAZON_PAAPI_PARTNER_TAG=yourtag-21
# AMAZON_PAAPI_HOST=webservices.amazon.in
# AMAZON_PAAPI_REGION=eu-west-1

# Optional (required only for image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional support contact shown on /contact
# SUPPORT_EMAIL=support@yourdomain.com
